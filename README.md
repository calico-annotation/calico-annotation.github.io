# calico-annotation.github.io

Landing page for CALICO, published at <https://calico-annotation.github.io>.

The site is a single static `index.html`. Every push to `main` deploys via the
GitHub Actions workflow in `.github/workflows/deploy.yml` (Pages source is set
to "GitHub Actions").

To update the page, edit `index.html` and push to `main`.
